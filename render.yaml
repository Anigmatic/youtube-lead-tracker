services:
  - type: web
    name: youtube-lead-tracker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --host=0.0.0.0 --port=$PORT wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
